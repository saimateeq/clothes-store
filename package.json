{
  "name": "velora",
  "private": true,
  "version": "0.0.0",
  "description": "VELORA — premium MERN fashion e-commerce platform",
  "scripts": {
    "dev": "concurrently -n SERVER,CLIENT -c blue,magenta \"npm:dev --prefix server\" \"npm:dev --prefix client\"",
    "dev:client": "npm run dev --prefix client",
    "dev:server": "npm run dev --prefix server",
    "install:all": "cd client && npm install --include=dev && cd ../server && npm install",
    "build": "npm run install:all && npm run build --prefix client",
    "start": "npm start --prefix server",
    "seed": "npm run seed --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
